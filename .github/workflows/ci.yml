name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: remixapp_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: 🛑 前回の実行をキャンセル
        uses: styfle/cancel-workflow-action@0.11.0

      - name: ⬇️ リポジトリをチェックアウト
        uses: actions/checkout@v4

      - name: ⎔ Node.jsをセットアップ
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn

      - name: 📥 依存関係をダウンロード
        run: yarn install --frozen-lockfile

      - name: 🔬 リント実行
        run: yarn lint

      - name: 🏗 ビルド実行
        run: yarn build
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test

      - name: 🌱 データベースセットアップ
        run: |
          yarn db:push
          yarn db:generate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test

      - name: ⚡ ユニットテスト実行
        run: yarn test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test

      - name: 🎭 E2Eテスト用開発サーバーを開始
        run: yarn dev &
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test
          SESSION_SECRET: test-session-secret

      - name: 🎭 開発サーバーの起動を待機
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:3000; do sleep 1; done' || exit 1

      - name: 🎭 Docker PlaywrightでE2Eテスト実行
        run: |
          # Playwrightサーバーを開始
          docker run -d \
            --name playwright-server \
            --network="host" \
            --rm \
            --init \
            --workdir /home/pwuser \
            --user pwuser \
            mcr.microsoft.com/playwright:v1.53.0-noble \
            /bin/sh -c "npx -y playwright@1.53.0 run-server --port 3001 --host 0.0.0.0"

          # サーバーの準備完了を待機
          timeout 60 bash -c 'until curl -f http://localhost:3001/ 2>/dev/null | grep -q "Running"; do sleep 1; done'
          
          # テストを実行
          PW_TEST_CONNECT_WS_ENDPOINT=ws://127.0.0.1:3001/ yarn playwright test --reporter=list
          
          # クリーンアップ
          docker stop playwright-server || true
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test
          SESSION_SECRET: test-session-secret

      - name: 📊 テスト結果をアップロード
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
