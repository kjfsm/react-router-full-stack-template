name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: remixapp_test
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: 🛑 Cancel Previous Runs
        uses: styfle/cancel-workflow-action@0.11.0

      - name: ⬇️ Checkout repo
        uses: actions/checkout@v4

      - name: ⎔ Setup node
        uses: actions/setup-node@v4
        with:
          node-version: 18
          cache: yarn

      - name: 📥 Download deps
        run: yarn install --frozen-lockfile

      - name: 🔬 Lint
        run: yarn lint

      - name: 🏗 Build
        run: yarn build
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test

      - name: 🌱 Database setup
        run: |
          yarn db:push
          yarn db:generate
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test

      - name: ⚡ Unit tests
        run: yarn test
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test

      - name: 🎭 Start dev server for E2E tests
        run: yarn dev &
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test
          SESSION_SECRET: test-session-secret

      - name: 🎭 Wait for dev server
        run: |
          timeout 60 bash -c 'until curl -f http://localhost:3000; do sleep 1; done' || exit 1

      - name: 🎭 Start Playwright Server
        run: |
          # Start Playwright server in background
          docker run -d \
            --name playwright-server \
            --add-host=hostmachine:host-gateway \
            -p 3000:3000 \
            --rm \
            --init \
            --workdir /home/pwuser \
            --user pwuser \
            mcr.microsoft.com/playwright:v1.53.0-noble \
            /bin/sh -c "npx -y playwright@1.53.0 run-server --port 3000 --host 0.0.0.0"

          # Wait for Playwright server to be ready
          timeout 30 bash -c 'until curl -f http://localhost:3000/ 2>/dev/null; do sleep 1; done'

      - name: 🎭 Playwright tests with remote server
        run: |
          # Run tests using environment variable to force remote connection
          PW_TEST_CONNECT_WS_ENDPOINT=ws://127.0.0.1:3000/ yarn playwright test --reporter=list
        env:
          DATABASE_URL: postgresql://postgres:postgres@localhost:5432/remixapp_test
          SESSION_SECRET: test-session-secret

      - name: 🎭 Stop Playwright Server
        if: always()
        run: docker stop playwright-server || true

      - name: 📊 Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
